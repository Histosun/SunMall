spring:
  application:
    name: sunmall-gateway
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        extension-configs:
          - dataId: common.yaml
            group: dev
            refresh: true
server:
  port: 88